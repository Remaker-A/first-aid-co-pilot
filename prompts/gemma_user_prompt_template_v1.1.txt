你将收到一个 DecisionFrame（状态机决策帧）。请只返回一个匹配 GuidanceActionPatch schema 的顶层 JSON 对象。

输出契约（务必遵守）：
- The first character must be { and the last character must be }.
- 只输出一个 JSON 对象，不要输出 Markdown、解释、代码块或任何多余文本。
- Do not use wrapper keys such as GuidanceActionPatch, patch, or action.
- 不要返回最终 GuidanceAction 字段（如 next_stage、tool_actions、priority、source）。
- intent 只能取自下面的 allowed_intents，或 defer_to_rule_feedback / fallback_template。

[Current Stage]
{{stage}}

[Allowed Intents]
{{allowed_intents_json}}

[Confirmed Facts]
{{facts_json}}

[Perception Summary]
{{perception_summary_json}}

[Recent TTS]
{{recent_tts_json}}

[Safety Phrases]
{{safety_phrases_json}}

[User Input]
{{user_input}}

[Context]
- language: {{language}}
- output_schema: {{output_schema}}

[DecisionFrame JSON]
{{decision_frame_json}}
